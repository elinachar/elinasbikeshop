<% if signed_in? %>
  <div class="row new-comment-thank-you">
    <p class="col-xs-12 text-center">Thank you for your review!</p>
  </div>
  <div class="row">
    <%= form_with(model: [@product, @product.comments.build], id: "new_comment") do |f| %>
      <div class="col-xs-12">
        <h4 class="new-review">Add a review</h4>
      </div>

      <div class="col-xs-12">
        <div class="form-group">
          <%= f.label :body, "Comments", class: "sr-only" %><br>
          <%= f.text_area :body, placeholder: "Your comments on the product...", class: 'form-control new-comment-text', size: '5x10'  %>
        </div>

        <div class="form-group">
          <div class="rating"></div>
        </div>

        <div class="actions">
          <%= f.submit "Submit", class: "btn btn-default" %>
        </div>
      </div>
    <% end %>
  </div><!-- end row -->

<% end %>
